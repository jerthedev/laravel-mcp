# Project Docker Configuration Template
# This file should be copied to your project root directory as 'docker.config'
# Settings defined here override global DockerManager defaults

# Project settings (REQUIRED)
PROJECT_PREFIX=packages

# Port settings (OPTIONAL - if not specified, will be auto-assigned starting from global BASE_PORT)
# BASE_PORT=8080

# Project type (OPTIONAL - defaults to Laravel)
# Currently supported: Laravel (Flutter support planned)
# PROJECT_TYPE=Laravel

# Database settings (OPTIONAL - inherits from global config if not specified)
# DB_DATABASE=myproject
# DB_USERNAME=root
# DB_PASSWORD=

# Container settings (OPTIONAL - inherits from global config if not specified)
# CONTAINER_RESTART_POLICY=unless-stopped

# Note: All commented settings will inherit values from ~/DockerManager/docker.config
# Only uncomment and modify settings you want to override for this project